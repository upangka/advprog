函数式编程就是：不修改任何已有数据，只通过传入参数，用纯函数计算出新数据；为了把多个计算串起来，允许把函数像变量一样传来传去。它本质上是一套“自我约束”，而不仅仅是一个语法糖。

函数式编程是一种编程范式，它通过“纯函数”的组合来处理数据，强调“不可变数据”和“无副作用”，而“函数作为参数/返回值”只是实现这种范式的手段之一。

“传函数”是形式（How），而“纯函数+不可变”是灵魂（What）。没有灵魂，形式再像也不是函数式编程。
